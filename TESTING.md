# BoneCode 试用清单

打开应用后按这个顺序过一遍，每一项都能对应到需求里的功能。
遇到问题记下「哪一步 + 现象」，我来修。

---

## 0. 启动窗口尺寸

- [ ] **首次打开时窗口应当占满屏幕的绝大部分**（留一圈边距），而不是小小一块
- [ ] 换到不同尺寸的显示器上，窗口大小应随之变化（大屏给大窗口）
- [ ] **自己拖动调整过窗口大小后**，下次启动应恢复你调整后的尺寸和位置
- [ ] 把窗口拖到副屏再关掉，重开应出现在副屏上（不会跑到看不见的地方）
- [ ] 窗口不应超出屏幕可见范围（不压到菜单栏或 Dock 下面）
- [ ] 窗口最小尺寸约 820×520，再小拖不动（否则分栏会挤坏）

## 0.5 左侧文件树

- [ ] 打开一个项目后，**文件树第一行就是项目文件夹名**（不是直接铺开里面的内容）
- [ ] **点这一行可以折叠 / 展开**，箭头随之变化
- [ ] 刚打开项目时根节点是**展开**的，能直接看到项目内容
- [ ] 折叠根节点 → 整个树收成一行；再展开 → 内容回来
- [ ] 右键项目根目录：「在 Finder 中显示」「复制路径」「在此处打开终端」可用
- [ ] **右键项目根目录：「移到废纸篓」「重命名」「创建副本」应当是灰的**
      —— 这几个动作会把整个项目搬走，必须禁止
- [ ] 右键项目里的普通文件/文件夹时，上面三个动作恢复正常可用
- [ ] 筛选框输入关键字 → 结果是平铺列表（不带根节点），清空后恢复成树

## 1. 编辑器（打开 `samples/Demo.java`）

- [ ] 语法高亮：关键字蓝色、字符串绿色、注释灰色斜体、注解黄色
- [ ] 行号在左侧，当前行有浅色底
- [ ] 把光标放到某个 `{` 或 `(` 旁边 —— 配对的括号会高亮
- [ ] 输入 `(` 会自动补上 `)`；在 `()` 中间按退格会一次删掉两个
- [ ] 回车时自动继承上一行缩进；在 `{` 后回车会自动多缩进一级
- [ ] 输入 `sout` 然后按 `⌃Space` —— 补全列表出现，回车插入 `System.out.println()`
- [ ] 输入 `fori` 试试代码片段；输入 `order.` 试试成员补全
- [ ] `⌘/` 切换注释，`⌘D` 复制当前行，`⌥↑`/`⌥↓` 上下移动行
- [ ] 选中多行按 `⌘⌥F` —— 按大括号层级重排缩进
- [ ] `⌘F` 打开查找条，支持增量搜索

## 2. Vue 混合语法（打开 `samples/Demo.vue`）

- [ ] `<template>` 里的标签和属性着色
- [ ] `<script setup>` 内部按 TypeScript 着色（`const` / `import` 是关键字色）
- [ ] `<style>` 内部按 CSS 着色（`#2f6feb` 是数值色，`14px` 也是）

## 2.5 图片与二进制文件

- [ ] **打开一张 `.png` / `.jpg`** —— 应显示图片本身，不是一屏乱码
      （以前会把 JPEG 的字节按 Latin-1 解码成乱码塞进文本编辑器）
- [ ] 底部信息栏显示 **像素尺寸 × 格式 × 文件大小**（如 `3024 × 1964 px · PNG · 2.1 MB`）
- [ ] 缩放按钮：`−` / `＋` / `适应窗口` / `实际大小`；快捷键 `⌘+` `⌘-` `⌘0` `⌘1`
- [ ] 透明背景的 PNG 应看到**灰白棋盘底**，而不是黑块或白块
- [ ] 图片小于窗口时居中显示，四周是棋盘底（不会被拉伸铺满）
- [ ] 大图按「适应窗口」缩小后，可以拖到「实际大小」看 1:1
- [ ] 图片标签页是**只读**的：标签上不出现「未保存」小圆点，关闭时不弹保存提示
- [ ] 重复打开同一张图片会**复用标签页**，不会越开越多
- [ ] 打开一个 `.zip` / `.class` / 可执行文件 —— 应给出「这是二进制文件」的说明
      和格式名（如「Mach-O 可执行文件」），并有一个「用系统默认应用打开」按钮
- [ ] **不会**出现乱码占位：二进制文件时文本区应该是空的
- [ ] 一个内容其实是文本、但扩展名是 `.png` 的文件（如 Git LFS 指针）仍按文本打开
- [ ] 带 BOM 的 UTF-16 文本文件能正常打开（中文不乱码）；保存后 BOM 不被改坏

## 3. Git 可视化（左侧栏切到「Git」）

- [ ] **变更**：改动一个文件后回到这里，能看到它出现在「已修改 · 未暂存」分组
- [ ] **三个分组要分开显示**（以前混在一起）：
      - 「已暂存 · 将随下次提交 (n)」—— `git add` 过的
      - 「已修改 · 未暂存 (n)」—— 已跟踪但改动还没 `add`
      - 「未跟踪 · 尚未加入 Git (n)」—— 新文件，Git 还没管（最容易被忽略的那批）
- [ ] **一眼看出是哪种变更**：每个文件前面有个带底色的小徽标 ——
      `新增`（绿）/ `修改`（蓝）/ `删除`（红）/ `重命名`（紫）/
      `新文件（未加入 Git）`（橙）/ `冲突`（深红），文件名也跟着变色
- [ ] 同一个文件既暂存又继续改（`MM`）时，两个分组里都会出现
- [ ] 点文件名 → 右侧打开差异对比页（增删行底色 + 行内词级高亮）
- [ ] 勾选/点击「暂存」→ 文件从「已修改」移到「已暂存」分组
- [ ] **每个 Git 按钮都要有回音**（以前点了完全没反应）：
      - 「全部暂存」/「取消全部」→ 状态栏出现「已暂存 N 个文件」之类的提示
      - 没选中任何文件就点「暂存」→ 提示「请先选中要暂存或取消暂存的文件」
      - 操作失败时弹窗说明原因，**不能静默吞掉**
- [ ] 写一句提交信息 → 「提交」→ 提交成功
- [ ] **点「提交」的瞬间就要有反应**（回归：点了看不出来）：
      - 按钮正上方立刻出现「正在提交…」，字色变深（不再是很淡的灰）
      - 四个按钮（提交 / 提交并推送 / 修正 / AI 生成）同时变灰
      - 项目有 pre-commit 钩子时会跑好几秒，这段时间必须有这个提示，
        否则会以为没点上而反复点
- [ ] 提交完成后提示消失、按钮恢复可用；提交信息框清空
- [ ] **提交被钩子拦下时**：弹出原因说明，按钮恢复可用（不会卡在灰着），
      并且**提交信息要保留**（不然白写了）
- [ ] 「AI 生成」也应有「正在生成提交信息…」提示并锁住按钮
- [ ] **如果项目配了 pre-commit 钩子**（husky / lint-staged）而检查不通过：
      弹窗应说明「提交被项目的 Git 钩子拦下了……不是 Git 报的错」，
      并提示可以用 `git commit --no-verify` 跳过
- [ ] 项目没有钩子时，普通的 Git 报错（如「无可提交」）**不应该**被说成钩子拦的
- [ ] 点「AI 生成」（需先配 AI）自动生成提交信息
- [ ] **历史**：提交图谱有彩色泳道连线，多选提交 → 右键「挑拣到当前分支」
- [ ] 右键提交 → 「还原」「重置到此」「从此处新建分支」「复制哈希」
- [ ] **分支**：看到 `main` 和 `feature/spring-sample`，双击可切换
- [ ] **贮藏**：点「贮藏当前改动」→ 改动被收走；再「弹出」恢复
- [ ] 顶部 `↑↓` 按钮：fetch / pull / pull --rebase / push
      —— 无论成功失败，下方输出面板都会自动展开并写明结果
- [ ] **没有配置上游分支时点「推送」** —— 应明确报告失败，不能只是没反应
- [ ] 「提交并推送」在推送失败时，要**分别**说明「提交成功」和「推送失败」
      （提交确实进了历史，只是没推上去）
- [ ] 冲突处理中的「继续 / 跳过 / 中止」三个按钮失败时要弹窗说明原因
      —— 尤其是「中止」失败会卡在 rebase 中间，必须让用户知道
- [ ] 「撤回 push」按钮（↩）里四条路径都能看到说明

## 4. 集成终端（`⌘\``）

- [ ] 按 `` ⌘` `` 展开终端，自动开一个 shell
- [ ] 输入 `ls`、`git log --oneline --graph` —— 颜色正常、图表正常
- [ ] `⌃C` 能中断正在跑的命令（说明是真 PTY，不是假终端）
- [ ] **输入中文**（比如 `echo 你好世界`）—— 应该正常，不会错位
- [ ] **把终端面板拖高/拖矮、或把窗口改宽改窄** —— 光标应始终贴在正确位置
      （以前 shell 不知道面板变宽了，换行位置会和光标错开）
- [ ] **光标的横坐标要对齐在提示符末尾** —— 不要在提示符右边空出一大段
      （以前字格宽度被向上取整，每字符多算一点，30 个字符累计偏出 2~3 格）
- [ ] 用 `⌘+` / `⌘-` 改终端字号，光标仍应贴在提示符末尾（每个字号都要对）
- [ ] 右上角 `+` 开第二个终端；`eraser` 清屏；`⏻` 重启
- [ ] **每个终端标签上都有 ×**，点 × 关掉的是那一个（不是当前选中的那个）
- [ ] 鼠标移到任意标签上，标签有浅色底、× 更明显
- [ ] **右键任意终端标签** → 出现「关闭 / 关闭其他终端 / 关闭全部终端 /
      重启这个终端」等选项，右键时会先切到那个标签
- [ ] 「关闭全部终端」后标签栏消失（面板保留，再按 `+` 可新建）
- [ ] **同一个运行配置点两次 Run** —— 应该切回原来那个标签并提示
      「已经在运行」，不会开出第二个同名标签
- [ ] 进程结束后再点 Run —— 在**原标签**里重新跑，不会另开一个
- [ ] 运行中的标签标题左侧有绿色小圆点；进程结束的小圆点消失
- [ ] 开 8 个以上终端 —— 标签栏内部可以横向滚动（滚轮/触控板），
      不会把右侧那排按钮挤出面板
- [ ] 右下角 ✨ 打开 AI 命令栏，输入「查看最近三次提交的改动」→ 生成命令

## 5. 运行项目

- [ ] 打开 `samples/spring` 这个子目录作为项目（⌘⇧O）
- [ ] 左侧「运行」面板应显示 **Spring Boot 运行**，并且识别到端口 8080
- [ ] 点运行 → 终端里跑起 `mvn`（需要本机有 maven 或 `mvnw`）
- [ ] 回到 `samples/Demo.vue` 所在目录，运行面板应识别出 Vue（需要 `package.json`）

## 6. AI 助手（`⌘⇧A`）

- [ ] 点右上角 ⚙️ 填入接口地址 + 模型名 + API Key
      （如 `https://api.deepseek.com/v1` / `deepseek-chat`）
- [ ] **输入框里按回车直接发送**；按 **Shift+回车** 换行（输入框左下角有提示）
- [ ] 没打开文件时点「解释代码」等快捷按钮 —— 面板里会就地说明原因，不是没反应
- [ ] 把 AI 面板拖窄，四个快捷按钮应变成两行两列且都能点（不会挤成一行点不到）
- [ ] 选中一段代码 → 点「解释代码」→ 回复流式输出
- [ ] 回复里带代码块时，底部出现「替换选中内容 / 插入到光标处」
- [ ] 点「替换选中内容」→ 编辑器内容被替换（可 `⌘Z` 撤销）

## 7. 标签页与欢迎页

- [ ] 多开几个文件，**右键任意标签页** → 出现「关闭 / 关闭其他 / 关闭右侧 / 关闭所有 /
      复制完整路径 / 在 Finder 中显示 / 重新从磁盘加载 / 在终端中打开所在目录」
- [ ] 「关闭其他标签页」只留当前这个；有未保存文件时会先弹一次保存提示
- [ ] 右键标签页时会先自动切到那个标签（符合直觉）
- [ ] 欢迎页（没有打开文件时）的「最近打开」列表：**鼠标移上去会有浅色高亮，
      光标变成手型**，点击即可打开
- [ ] **欢迎页的快捷键清单每一条都要完整显示** —— 特别是 `⌃Space`，
      以前会被切成 `⌃Spa`（`⌃` 是 U+2303，不是普通字符）
- [ ] 把窗口拖窄到接近最小尺寸，快捷键清单仍然完整（两列有固定宽度，不会互相挤）

## 8. 运行项目的可见反馈

- [ ] 点工具栏 ▶️ 运行（或侧栏「运行」面板里的 ▶️）
- [ ] **终端面板应自动展开并显示命令输出** —— 之前它会静默在折叠的面板里跑，
      看起来像没反应
- [ ] 状态栏显示「已启动「xxx」，输出在下方终端面板」
- [ ] **运行中时：▶️ 变灰不可点，⏹ 变红可点，旁边出现绿色「● 运行中」**
- [ ] 进程结束后（或关掉那个终端标签）三者自动恢复初始状态
- [ ] 停止按钮（⏹）能中断正在跑的进程（工具栏和终端面板各有一个，都要试）
- [ ] 若第一次点击没结束（有些 dev server 会吞掉 SIGINT），**再点一次会强制结束**
- [ ] 进程结束后 ▶️ / ⏹ / 「● 运行中」三者自动恢复初始状态

## 9. 其他

- [ ] `⌘P` 快速打开文件（输入 `hili` 试试模糊匹配 `SyntaxHighlighter.swift`）
- [ ] **快速查找打开后，点界面任何其他地方应该自动关闭**
- [ ] `⌘⇧F` 项目内搜索（搜 `forkpty`），点结果跳转到行
- [ ] `⌘⌥T` 切换深色/浅色主题 —— 编辑器、终端、Git 面板全部跟着变
- [ ] **重点看左侧边栏和右侧 AI 面板**：以前它们会被系统毛玻璃层盖住，
      跟着桌面壁纸走（壁纸深色时浅色主题下也是黑的），现在应该完全跟随主题
- [ ] **拖动左右两条分栏线要跟手**：慢拖、快拖、来回拖，宽度应该线性跟随鼠标，
      不会「拖不动」或「弹回去」（之前用系统分栏控制器，拖动是静默失效的）
- [ ] 侧边栏可缩到约 150pt、AI 面板约 220pt，再往里拖会折叠；双击分栏线不会误折叠
- [ ] **把窗口从窄拖到很宽**：多出来的空间应该全部给中间的编辑区，
      左右两个面板的宽度纹丝不动（以前富余空间会塞给最宽的面板，
      结果窗口一拉宽 AI 面板就自己涨到上限）
- [ ] **把窗口拖窄**：先挤中间的编辑区，编辑区到最小宽度后才轮到两侧让位
- [ ] **把窗口缩到最小（约 820pt 宽）**：右侧 AI 面板应完整可见，不能被窗口边缘切掉
- [ ] 工具栏所有图标按钮：鼠标移上去有浅色底、按下有更深色、光标变手型
- [ ] 终端与 AI 的工具栏按钮在面板打开时保持高亮，关闭后恢复
- [ ] `⌘+` / `⌘-` 调字号
- [ ] `⌘0` 收起侧边栏；`` ⌘` `` 收起终端
- [ ] 关闭窗口时若有未保存文件，会提示保存
- [ ] 每个标签页有关闭按钮（未修改时悬停出现 ×，已修改时显示小圆点）
      —— 小圆点在**标题左侧**，× 始终可用，不会被圆点顶掉
- [ ] **刚启动、还没打开终端时**：在「活动监视器」里看 BoneCode 的内存占用，
      不应该因为一个没展开的终端面板就多出一大块（终端视图是按需构建的）
- [ ] 第一次展开终端（`` ⌘` ``）应该正常出现 shell，且主题是当前主题
      （面板视图是那一刻才构建的，构建后要立刻跟随主题）

---

## 已知不做的事（别当成 bug）

- 没有跨文件跳转/重命名（没有接 LSP）
- 终端里不能用鼠标点选（`vim` 点选、`htop` 点击不支持）
- `git push` 要输密码时会失败 —— 先在系统终端 `git push` 一次缓存凭据
- 打开超过 12 MB 的文件会拒绝加载
- 图片只做查看，不能裁剪/旋转/编辑；矢量图 `.svg` 按文本打开（它是 XML）
